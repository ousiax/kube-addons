[OUTPUT]
    name            es
    match           *
    host            ${ES_HOST}
    port            ${ES_PORT}
    replace_dots    on
    retry_limit     false
    logstash_format on
    logstash_prefix fluent-bit
