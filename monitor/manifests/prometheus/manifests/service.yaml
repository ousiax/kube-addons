apiVersion: v1
kind: Service
metadata:
  name: prometheus
  labels:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/part-of: monitor
spec:
  selector:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/part-of: monitor
  ports:
    - port: 9090
      targetPort: 9090
